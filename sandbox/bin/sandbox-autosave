#!/usr/bin/env bash
# Safety net: every SANDBOX_AUTOSAVE_MINUTES run sandbox-save.
source sandbox-lib
interval="${SANDBOX_AUTOSAVE_MINUTES:-10}"
while true; do
	sleep "$((interval * 60))"
	sandbox-save 2>&1 | grep -v -E "^nothing to push$" | sed "s/^/$(date '+%Y-%m-%d %H:%M:%S') /" || true
done
